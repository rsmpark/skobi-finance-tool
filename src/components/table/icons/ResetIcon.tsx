import MuiClearIcon from "@mui/icons-material/Clear";
import { SvgIconProps } from "@mui/material";

const ClearIcon = (props: SvgIconProps) => {
  return <MuiClearIcon sx={{ fontSize: "18px" }} {...props} />;
};

export default ClearIcon;
