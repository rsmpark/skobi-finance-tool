import MuiNumbersIcon from "@mui/icons-material/Numbers";
import { SvgIconProps } from "@mui/material";

const NumbersIcon = (props: SvgIconProps) => {
  return <MuiNumbersIcon sx={{ fontSize: "20px" }} {...props} />;
};

export default NumbersIcon;
