import MuiAddIcon from "@mui/icons-material/Add";
import { SvgIconProps } from "@mui/material";

const AddIcon = (props: SvgIconProps) => {
  return <MuiAddIcon sx={{ fontSize: "14px" }} {...props} />;
};

export default AddIcon;
